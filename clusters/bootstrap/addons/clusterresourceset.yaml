apiVersion: addons.cluster.x-k8s.io/v1beta2
kind: ClusterResourceSet
metadata:
  name: bootstrap-cni-and-taint
spec:
  clusterSelector:
    matchLabels:
      cluster-role: workload
  resources:
    - kind: Secret
      name: bootstrap-cni-manifests
    - kind: Secret
      name: taint-remover-job
